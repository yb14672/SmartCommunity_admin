<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zy_admin.community.entity.ZyRoom">
    <select id="checkRoomByName" resultType="com.zy_admin.community.entity.ZyRoom">
        select * from zy_room
        where community_id=#{zyRoom.communityId}
        and building_id=#{zyRoom.buildingId}
        and unit_id=#{zyRoom.unitId}
        and room_level=#{zyRoom.roomLevel}
        and room_name=#{zyRoom.roomName}
    </select>

</mapper>